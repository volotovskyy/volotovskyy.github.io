// vendor
import React from "react";

// proj
import { Layout } from "layouts";
import { PostsFeed } from "components";

export const PostsFeedPage = props => {
  return (
    <Layout>
      <PostsFeed />
    </Layout>
  );
};
