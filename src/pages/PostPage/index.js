// vendor
import React from "react";

// proj
import { Layout } from "layouts";
import { Post } from "components";

export const PostPage = () => {
  return (
    <Layout>
      <Post />
    </Layout>
  );
};
